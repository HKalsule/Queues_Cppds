#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a[] = {1,2,3,4,5};
    queue<int> q;
    
    for(int i=0;i<5;i++)
    {
        q.push(a[i]);
    }
    
    cout << "The elements of the queue are : ";
    
    while(!q.empty())
    {
        cout << q.front() << " ";
        q.pop();
    }
    return 0;
}
